<%- include('../partials/header') -%>
    <div class="flex flex-row justify-center items-center bg-zinc-800 h-screen w-full px-10">
        <div class="flex flex-col p-5 mt-5 min-w-1/3 h-fit bg-rose-100 text-[#A87677] rounded-lg outline-none">
            <class class="text-3xl font-sans font-extrabold py-5 text-center w-full">
                Login to your account
            </class>
            <form class="flex flex-col gap-3" action="/auth/login" method="post">
                <label for="email">Enter email</label>
                <input class="border border-[#A87677] rounded-md p-2" type="text" name="email" placeholder="email"/>
                <label for="password">Enter password</label>
                <input class="border border-[#A87677] rounded-md p-2" type="password" name="password" placeholder="password"/>
                <input type="submit" class="bg-[#A87677] text-rose-100 hover:bg-[#A87677]/80 hover:cursor-pointer rounded-md p-2" value="Login">
            </form>
            <div class="font-extrabold text-black text-center mt-2"> OR </div>
            <a href="/auth/google" class="bg-[#A87677] text-rose-100 hover:bg-[#A87677]/80 hover:cursor-pointer rounded-md p-2 text-center">Login via Google</a>
            <% if(locals.errors && locals.errors.length > 0) { %>
                <div class="text-red-500 mt-2">
                    <% locals.errors.forEach(error => { %>
                        <p><%= error.msg %></p>
                    <% }) %>
                </div>
            <% } %>
            <div class="flex flex-row gap-2 text-zinc-900 mt-10">
                <p>Don't have an account? </p> <a href="/register" class="text-[#A87677] hover:underline">Register</a>
            </div>
        </div>
    </div>
<%- include('../partials/footer') -%>