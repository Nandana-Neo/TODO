<%- include('../partials/header') -%>
    <div class="flex flex-col gap-4 bg-zinc-800 h-screen w-full p-10">
        <div class="flex flex-col gap-4 bg-rose-100 p-4 rounded-lg text-[#A87677]">
            <div class="flex flex-row gap-2">
                <div class="font-extrabold">Name:</div> <div><%= user.name %></div>   
            </div>
            <div class="flex flex-row gap-2">
                <div class="font-extrabold">Email:</div> <div><%= user.email %></div>
            </div>
            <a class="bg-[#A87677] text-rose-100 hover:bg-[#A87677]/80 hover:cursor-pointer rounded-md p-2 w-full mt-5 text-center" href='auth/profile/edit'>Edit</a>
        </div>
    </div>
<%- include('../partials/footer') -%>