    </div>
    <div class="fixed bottom-6 right-6 z-50 flex flex-col items-center gap-2">
        
    <form action="/jwt/toggle-auth-mode" method="POST">
        <button 
        type="submit"
        class="relative inline-flex h-8 w-14 items-center rounded-full transition-colors duration-300 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-rose-500
        <%= isJwtMode ? 'bg-rose-300' : 'bg-gray-600' %>"
        >
        
        <span
            class="inline-block h-6 w-6 transform rounded-full bg-white transition duration-300 ease-in-out
            <%= isJwtMode ? 'translate-x-7' : 'translate-x-1' %>"
        ></span>
        
        </button>
    </form>

    <span class="text-xs font-bold uppercase tracking-wider px-2 py-1 rounded bg-zinc-800 text-white shadow-lg border border-zinc-700">
        <%= isJwtMode ? 'Token Mode' : 'Session Mode' %>
    </span>

    </div>
</body>

</html>